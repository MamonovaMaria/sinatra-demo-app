h1 Админка

a.right.btn.btn-primary href="/admin/new" Создать страницу

row
  table.table.table-striped
    thead
      tr
        th id
        th page_url
        th page_title
        th published
        th parent_id
        th actions
    tbody
      - @pages.each do |page|
        tr
          td =page.id
          td =page.page_url
          td =page.page_title
          td =page.published
          td =page.parent_id
          td
            a.left.btn.btn-xs.btn-warning href="/admin/edit/#{page.id}" Редактировать
            form action="/admin/destroy" method="post"
              button.left.btn.btn-xs.btn-danger name="destroy" value="#{page.id}" Удалить
