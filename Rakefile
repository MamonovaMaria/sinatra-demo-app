# указываем откуда Rake будет брать таски
Dir.glob('tasks/*.rake').each { |r| import r }

# указываем какое действие будет выполняться по-умолчанию
task :default do
  puts "Для просмотра доступных тасков введите: \"bundle exec rake -T\""
end
